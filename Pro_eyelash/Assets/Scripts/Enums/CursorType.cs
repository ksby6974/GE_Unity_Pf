namespace chataan.Scripts.Enums
{
    // ━━━━━━━━━━━━━━━━━━━━━━━━━━
    // 마우스 커서 종류
    // ━━━━━━━━━━━━━━━━━━━━━━━━━━
    public enum CursorType
    {
        normal = 0,     // 기본
        check = 1,      // 선택
    }
}